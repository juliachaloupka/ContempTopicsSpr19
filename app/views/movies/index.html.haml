-#  This file is app/views/movies/index.html.haml
%h1 All Movies


%table#movies
  %thead
    %tr
      %th Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.movie_title
        %td= link_to "More about #{movie.movie_title}", movie_path(movie)


= form_tag movies_search_db_path, :method => :post, :id => 'search_form' do
  = label 'search', :search_terms, 'Search for movies titles containing:'
  = text_field nil, :search_terms, :id => 'search_box'
  = submit_tag 'Search db', :id => 'search_button'
